schema_version = "field-policy-0.1.0"
policy_version = "field-policy-0.1.0"

# Natural-language conversation content is eligible for local extraction.
# Tool descriptions and all argument values require an explicit, reviewed policy.
tool_description_action = "manual_policy_required"
parameter_description_action = "manual_policy_required"
argument_policies = []
